## Local setup

Copy the environment template and replace every `changeme` value with a local
credential before starting the services:

```sh
cp .env.example .env
docker compose up
```

`.env` is ignored by Git. Do not commit it.
